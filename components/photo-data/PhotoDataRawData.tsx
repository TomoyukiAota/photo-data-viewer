import classes from './PhotoDataRawData.module.scss';

const PhotoDataRawData: React.FC<{ className?: string }> = (props) => {
  return <div>Photo Data Raw Data</div>;
};

export default PhotoDataRawData;
