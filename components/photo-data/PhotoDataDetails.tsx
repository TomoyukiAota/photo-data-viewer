import classes from './PhotoDataDetails.module.scss';

const PhotoDataDetails: React.FC<{ className?: string }> = (props) => {
  return <div>Photo Data Details</div>;
};

export default PhotoDataDetails;
